apiVersion: v1
kind: Service
metadata:
  name: webhook-ingest-nodeport
  labels:
    app: webhook-ingest
spec:
  type: NodePort
  ports:
  - port: 9091
    targetPort: 9091
    nodePort: 30091
  selector:
    app: webhook-ingest
